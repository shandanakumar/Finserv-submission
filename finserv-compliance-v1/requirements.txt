# ============================================================
# FinServ Compliance Assistant — Python Dependencies
# Tested on Python 3.11
#
# WHAT CHANGED FROM OLLAMA VERSION:
#   REMOVED: ollama==0.2.*
#   ADDED:   boto3, botocore (AWS SDK — handles Bedrock calls)
#   ADDED:   awscli (for `aws configure` and credential setup)
# ============================================================

# ── AWS ──────────────────────────────────────────────────────
# boto3: AWS SDK for Python. Handles auth, retries, signing.
# botocore: lower-level foundation that boto3 sits on top of.
# awscli: command-line tool for `aws configure`, `aws bedrock list-foundation-models`
boto3==1.34.*
botocore==1.34.*
awscli==1.32.*

# ── Web framework ─────────────────────────────────────────────
fastapi==0.111.*
uvicorn[standard]==0.29.*
pydantic==2.7.*
pydantic-settings==2.2.*
python-multipart==0.0.*

# ── HTTP client ───────────────────────────────────────────────
httpx==0.27.*
aiohttp==3.9.*

# ── Vector store ─────────────────────────────────────────────
qdrant-client==1.9.*

# ── Reranker (local — no Bedrock needed, runs on CPU) ─────────
sentence-transformers==3.0.*

# ── BM25 sparse retrieval ─────────────────────────────────────
rank-bm25==0.2.*

# ── Document parsing ──────────────────────────────────────────
pdfminer.six==20231228
PyPDF2==3.0.*
python-docx==1.1.*

# ── LangGraph + LangChain ─────────────────────────────────────
langgraph==0.2.*
langchain-core==0.2.*

# ── PII redaction ─────────────────────────────────────────────
presidio-analyzer==2.2.*
presidio-anonymizer==2.2.*
spacy==3.7.*

# ── Evaluation ────────────────────────────────────────────────
ragas==0.1.*
datasets==2.19.*

# ── Utilities ────────────────────────────────────────────────
python-dotenv==1.0.*
tenacity==8.3.*
structlog==24.1.*
numpy==1.26.*
tqdm==4.66.*

# ── Testing ───────────────────────────────────────────────────
pytest==8.2.*
pytest-asyncio==0.23.*
